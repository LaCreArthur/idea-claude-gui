{
  "name": "ai-bridge",
  "version": "2.0.0",
  "description": "Minimal Claude SDK bridge using stdin/stdout JSON protocol",
  "type": "module",
  "dependencies": {},
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.16"
  }
}
